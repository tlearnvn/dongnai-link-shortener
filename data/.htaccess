# Chặn mọi truy cập từ web vào thư mục dữ liệu.
# Tệp cơ sở dữ liệu SQLite nằm ở đây và chỉ PHP được phép đọc/ghi.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
